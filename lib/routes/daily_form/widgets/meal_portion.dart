import 'package:flutter/material.dart';

class MealPortion extends StatelessWidget {
  final String sizeText;

  const MealPortion({Key? key, required this.sizeText}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
